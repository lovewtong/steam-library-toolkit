# PR #5 审核修复记录（2026-09-15）

审查基线为 ddf0844。本轮处理审核的合并阻塞项 F01、F02、F03、F05，并修复来源差异语义 F04。其余发现保留为后续工作，不因本轮测试通过而视为关闭。

| 编号 | 修复 | 验证 |
|---|---|---|
| F01 | HTTP 尝试在独立子进程中执行，父进程统一计算排队限速、请求与重试的 35 秒预算；取消时终止子进程；响应体上限 8 MiB | 本地回环服务持续发送字节，2 秒测试预算触发 Timeout；请求进程已退出；补全中断取消在途请求并保持旧指针；超大体与重定向测试 |
| F02 | Retry-After 冷却进入进程共享 gate；当前请求预算不足以等待时，其他 AppID 标为 deferred，不请求、不写入失败缓存 | 429/Retry-After=120 后连续处理三个 AppID，只启动第一个请求，后两个延期 |
| F03 | collect/enrich 共用 apply_observation；不可变审计保存 fields、applied_fields、field_actions、source、fetched_at、read_at；无字段应用时不写 store_metadata 来源 | 直接采集并发布 {} 与 genres=[]，冻结证据分别为 missing/empty；缓存时间 100、本轮读取时间 200 保持区别 |
| F04 | 只有已核验客户端与可用 API 才计算确定差集；比较不可用时 count=null、存在性 null，另存 comparison 状态 | API 禁用或失败不产生确定差集；成功返回空 API 列表仍能产生真实差集 |
| F05 | 有效正向类别 ID 不因其他条目坏格式丢失；不完整列表没有正向依据时返回 unknown | 日文描述的 ID 28 与缺 ID 条目混合，手柄为 true，多人为 null，旧已知值不被 false 覆盖 |

请求进程通过私有管道接收 URL/参数，不把它们放入命令行或异常信息。不跟随重定向；禁用 Requests 准备重定向时隐式读取响应体的行为。总预算到期后终止进程，另允许至多 2 秒进程回收等待；这不是操作系统异常情况下的硬实时保证。补全的整批排队时间不计入单个请求预算，尚未启动的工作会在取消时撤销。

同进程中冷却生效之前已经发出的请求无法撤回，多个进程尚不共享冷却。长期冷却会导致 partial/deferred，稍后重跑可继续。缓存升到 v5，旧 v4 混合格式的错误结果会按需重新获取。

## 复审 F11 / F12

复审基线 f0682b1 的 F11、F12 已针对性修复：内存 HTTP 响应标记为已消费，关闭时不再访问不存在的 raw；采集补齐类型时采用实际观察来源，并在 fields、applied_fields、field_actions 中保存类型更新和值。

F11 的新增真实 worker 回环测试在修改前复现 AttributeError；修改后 503→200、短冷却 429→200 均收到第二次请求并成功，503/429 耗尽三次后返回对应 HTTP 状态，响应可在读取内容前安全关闭。F12 的直取与缓存命中测试检查发布后的不可变审计，来源分别为 store_api、store_cache。

## 尚未关闭

- F08：最近游玩时间在 CSV/Markdown 中的独立历史状态。
- F09：Node/Python 对 API null 的一致契约。
- F10：旧收藏入口的空集合、dry-run 写入和写回前验证。

继续保持旧收藏写回为未完成验收的实验路径。本轮没有执行 Steam 认证、账号切换、Families、退款/移除或收藏写回测试。

## 既有实测的证据范围

此前 229 条手柄纠正和 572.493 秒性能数据来自 2026-09-13 的运行。原始产物在用户的 Codex 工作目录下，并非项目根目录；外部审核如果没有这组路径，就无法独立复核相应结论。复核应使用补全前后 current 指针，校验全部文件哈希，再比对原始记录、父运行与分类产物。

这些历史耗时不适用于加入请求子进程后的版本；新版本有进程启动与管道开销。本轮以离线故障复现为主要验收，尚未重新进行全库多轮性能基准和真实认证验收。

本轮复核上述历史产物的所有 manifest 哈希通过，确认 388 条记录、229 条手柄变化及已保存分类的“其他”14→3。最终历史结果 producer=ddf0844、dirty=false；之前的基线和实时补全中部分 producer 为 dirty=true，因此不能把所有历史样本视为某个干净提交的集成验收。

本轮本地验证：98 项 Python、20 项 Node 测试通过，源码凭据模式扫描及 diff 检查通过；包含原有截止、取消、worker 回收及旧指针保护回归。此前公开 AppID 500 商店请求成功；本次 F11/F12 仅使用本地回环和离线数据，未执行 Steam 认证或全库真实性能测试。测试通过不代表所有网络故障或账号场景均已验收。

## PR #5 之后的分类整改

a923f50 的复审与可合并评估见 [PR5_REVIEW.md](PR5_REVIEW.md)，没有执行合并。
F06/F07 在独立本地分支 fix/classification-quality-evidence 处理，不能将新行为归为 a923f50 已有功能。

F06 的两条已确认误分类、缺少依据却默认动作、Racing 等基础映射差异及表格标题子串误判已有针对性修复。
F07 的主类/子类变化导致旧细项失效、逐字段依据及发布和展示已修复。验证 106 项 Python、22 项 Node 本地通过，具体范围见 [分类核对记录](CLASSIFICATION_REVIEW.md)。

F06/F07 的已报告复现可关闭；所有 KNOWN 迁移、全库人工样本评估和细分类统一仍为分类质量后续事项。F08–F10 与真实性能、账号场景仍开放。
