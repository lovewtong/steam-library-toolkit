# 元数据覆盖与补全性能

## 使用

```powershell
.\.venv\Scripts\python.exe steam_enrich.py --input steam_enriched.json -o steam_metadata.json --workers 2
```

需要已核验的运行指针；使用独立输出，不重新登录 Steam，也不改变成员、应用类型、时长及采集时间。`--appid` 可重复以限制补全范围，`--cache-dir` 可选择缓存目录。默认读取有效缓存，`--refresh-metadata` 强制请求商店。

## 字段正确性

商店请求成功并不意味着字段完整。旧版以中文/英文文字匹配支持能力，例如漏识别“完全支持控制器”，曾将支持手柄的应用标成 false。现在优先使用类别 ID：多人/合作为 1、9、27、36–39、47–49；手柄为 18、28，并接受明确的 controller_support=full/partial。缺少完整 ID 列表时兼容文字判断。这是对当前商店响应的解释，不是 Valve 对该接口的稳定性承诺。

- genres/categories 分别记录 present、empty、missing、invalid。
- 缺失或格式错误不覆盖已有字段；明确空数组可以替换旧数组。
- 支持能力为 true、false、null，null 不覆盖已有已知值。
- 每个成功响应记录 fields 和 applied_fields；未应用的字段保留以前的观察值，不表示再次核实。
- 总库 coverage_before/coverage_after 分别统计非空标签、空或未知标签、支持能力的 true/false/unknown。旧运行的空数组没有足够证据区分空与未知，因此合并统计，不能声称已知为空。

审计在当前运行的 `steam_library.audit.json` → metadata 中。采集与独立补全均保存 fields、applied_fields、field_actions、实际来源，以及 fetched_at（取得响应的 Unix 秒时间）和 read_at（本轮读缓存/结果的 Unix 秒时间）。`state=complete` 仅表示全部选中请求成功；判断可用字段必须同时看覆盖率与逐项字段状态。

## 性能与缓存

独立补全默认 2 个线程，`--workers` 可选 1–4；采集器内的商店补全仍为串行。所有商店线程在同一进程内共享至少 1.5 秒的请求启动间隔，重试也经过此限制。间隔与网络等待重叠，不再每个请求完成后额外等待 1.5 秒。HTTP 层继续有限重试、退避和 Retry-After；这不是官方速率保证，多个进程也不共享这个限速器。

工作线程只负责读取，主线程按源库顺序归并与发布。中断时取消排队任务、等待在途请求退出，只有完整发布成功后才切换指针。缓存可复用已完成请求，失败不会删除库成员。

缓存格式现为 v5，使旧文字及混合格式误判不能继续复用；旧 v3/v4 缓存将按需重新获取，首次升级可能较慢。成功缓存有效期 7 天，not_found 6 小时；访问拒绝、限流、解析/网络失败使用更短有效期。not_found 不证明永久下架或账号无权使用。

2026-09-14 审核修复：Retry-After 进入共享冷却，来不及等待的工作标为 deferred 且不发请求；独立请求进程提供总预算与可终止的响应体读取，默认上限 8 MiB。预算包含本次请求的 gate 等待和重试，不包含整个补全批次的排队时间。正常预算为 35 秒，进程回收另有至多 2 秒等待；取消信号在等待与读取期间检查。详细修复与限制见 [AUDIT_REMEDIATION.md](AUDIT_REMEDIATION.md)。下面的性能数字来自修改前历史版本，不代表加入子进程后的吞吐量。

## 2026-09-13 小样本实测

同机公开商店接口，8 个 AppID（500、550、730、241930、770720、326480、627270、15150），每轮 5 个成功、3 个不可用：

| 路径 | 耗时 |
|---|---:|
| 修改前串行，强制请求，仅请求/缓存部分 | 16.603 秒 |
| 新单线程，强制请求，含发布 | 12.102 秒 |
| 新双线程，强制请求，含发布 | 12.065 秒 |
| 新双线程，8 项缓存命中，含发布 | 1.042 秒 |

测量不是重复统计实验，且旧版时间不含发布。双线程在本次快速响应样本中与新单线程接近，主要收益来自等待重叠与缓存；网络较慢时可并发等待，不能承诺固定倍数加速。样本纠正 3 条手柄支持误判，所有成员、类型和时长证据保持不变。

回归覆盖字段缺失/无效/明确空、中文类别 ID、文字兼容、共享启动间隔、重试限速、线程实际重叠、结果顺序、取消与旧指针保留。

## 全库验证

同日对已有 388 条记录执行双线程补全，8 条缓存命中，耗时 572.493 秒；375 条成功，13 条 not_found。由于商店元数据仍有不可用项目，状态为 partial。

| 字段 | 补全前 | 补全后 |
|---|---:|---:|
| 非空 genres / categories | 各 370 | 各 370 |
| 手柄支持 true | 17 | 246 |
| 手柄支持 false | 353 | 124 |
| 手柄支持 unknown | 18 | 18 |
| 多人支持 true / false / unknown | 125 / 245 / 18 | 125 / 245 / 18 |

共 229 条手柄支持结果发生纠正。非空标签没有增加，本轮收益主要是支持能力正确性、字段缺失保护与可核验的覆盖报告。库仍为 388 条、377 个 game、27 条未知时长；逐字段比对确认成员、类型、时长和采集证据没有变化。人工分类规则使“其他”剩余 3 项，不能据此推断商店缺失元数据已经恢复。
