# 第一阶段验收记录（2026-09-12）

本轮完成发布前检查、干净环境验证、全库商店补全及分类质量评估。个人库、缓存和逐应用验收明细仅保存在本机，未加入仓库。

## N01：发布前检查

- 检查了本地可达 Git 历史中的 106 个 blob，以及待发布文件清单；JWT、API Key、私钥、GitHub token、凭据字面量等模式没有发现实际凭据。命中的测试占位值和 SteamID 转换常量已复核。
- 当前跟踪文件不含个人配置、真实游戏库、登录缓存或生成的运行产物。模式扫描不是穷尽性保证，也不覆盖不可达对象或被忽略的本机文件。
- 旧 `import_script.js` 中的固定 AccountID 已存在于公开 main。新代码改为显式设置 `STEAM_ID_32`，缺失、零值、非法值或超出 32 位范围时在写入前退出；没有重写历史，没有对真实收藏执行写入。

## N02：干净环境

- 从提交版本创建独立 Git clone，未复制个人配置、node_modules 或原虚拟环境。
- 新建 Python 3.14.2 虚拟环境，安装 `requirements-tested.txt`，执行 `npm ci`（Node 22.19.0）。`pip check` 通过。
- 63 项 Python、17 项 Node 测试通过；诊断、补全和 picker 命令入口通过。
- GitHub 的 Python 3.12 / Node 22 离线矩阵已在 Windows、Linux、macOS 通过。首次验证运行：[PR checks](https://github.com/lovewtong/steam-library-toolkit/actions/runs/34686106846)。真实 Steam 认证仍只验证了 Windows。

## N03：全库补全

| 指标 | 结果 |
|---|---:|
| 输入与输出成员 | 388 / 388 |
| 默认 game 分类 | 377 |
| 商店详情成功 | 376 |
| 商店详情未取得（适配器记为 not_found） | 12 |
| 缓存命中 | 3 |
| 补全耗时 | 778.93 秒（约 13 分钟） |
| 已知 / 未知时长 | 361 / 27 |

逐字段核验成员、类型、时长与其他非元数据值不变，来源证据和原始采集时间保留。全部运行产物哈希通过校验，picker HTTP API 返回同一运行的 377 个游戏。

补全状态为 `partial`，12 条记录继续保留。`not_found` 是当前适配器对“没有可用商店详情”的归类，不能直接证明下架或区域限制；本次没有证据区分这些具体原因。

## N04：分类覆盖与质量

| 指标（377 个 game） | 补全前 | 补全后 |
|---|---:|---:|
| 表格主分类为“其他” | 366 | 14 |
| 缺少 genres | 377 | 14 |
| 五维分类的通用细分“多种元素” | 108 | 92 |
| 五维分类的通用氛围“风格各异” | 112 | 82 |
| 命中名称规则表 | 265 | 265 |

14 个“其他”中，12 个未取得商店详情，另 2 个请求成功但没有 genres。获取详情成功不等于字段齐全。以上是覆盖指标，没有人工标注的完整基准集，不能转化为分类准确率。

抽查发现两个分类器采用不同粒度：例如同一应用在表格中依据商店 Action 进入“动作/冒险”，在 picker 的已知名称规则中进入“射击”。另一个样本未命中已知名称表，只能给出通用动作分类。这不影响成员集合，但会影响用户对筛选结果的预期。

后续具体任务：

1. 建立按 AppID 维护的人工校正与证据字段，避免名称变更、商标字符或本地化造成匹配遗漏。
2. 明确两种分类输出的粒度与映射关系，并为代表性游戏建立人工复核样本集。
3. 在补全审计中区分“请求成功”和“各字段可用”，让成功但缺 genres 的项目可直接定位。
4. 为缺少商店详情的项目提供有来源的手工元数据覆盖；没有证据时继续保留未知。
5. 以此次约 13 分钟为性能基线，评估全局限速下的有限并发，不能靠增加请求量掩盖限流。

## N05 / N06：远端交付

- 已核对并更新现有 [PR #2](https://github.com/lovewtong/steam-library-toolkit/pull/2)，说明覆盖完整采集、审计、picker 与独立补全改动。
- 修复分支已推送，Windows/Linux/macOS 离线 CI 已通过。后续提交及合并状态以该 PR 的实际记录为准。
- 本阶段没有创建版本号或附带个人数据的 release 附件。

## 仍未解决的边界

27 条未知时长、客户端绝对完整性证明、家庭共享及权益变化、多账号/多设备真人切换、macOS/Linux 真人认证仍需后续专项验证。旧收藏写回虽然已去除固定账号，但持久化与同步行为仍未完成独立治理。
