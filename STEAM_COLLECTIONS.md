# 在 Steam 应用里给游戏分类（收藏）

Steam 客户端**不支持**从外部批量写入我们的五维分类，只能通过 **收藏（Collections）** 在库内手动分组。

---

## 1. Steam 里怎么做

1. 打开 **Steam 客户端** → 进入 **库**。
2. 菜单 **视图** → **收藏** → **创建新收藏**（或右上角「+」）。
3. 输入收藏名称（例如：`低强度`、`解压`、`肉鸽`）。
4. 在库中**右键游戏** → **添加到收藏** → 选择刚建的收藏；或勾选多款游戏后一次性添加。

这样就能在库左侧按收藏筛选游戏。

---

## 2. 用我们的分类做「对照清单」

我们的分类**不能直接写进 Steam**，但可以导出**按维度分组的游戏名单**，你在 Steam 里建同名收藏后，按名单往里加游戏即可。

### 导出收藏清单

```bash
python steam_picker.py --export-collections
```

会生成 **steam_collections_guide.md**，内容包括：

- **按强度**：低 / 中 / 高，每个下列出游戏名。
- **按核心玩法**：动作冒险、策略、射击等，每个下列出游戏名。
- **按细分流派**：肉鸽、类魂、塔防等，每个下列出游戏名。
- **按氛围**：解压、黑暗、赛博朋克等，每个下列出游戏名。

### 操作步骤

1. 运行上面的命令，打开 `steam_collections_guide.md`。
2. 在 Steam 里按你需要的维度建收藏（例如建「低强度」「解压」「肉鸽」）。
3. 对照 MD 里每个小节下的游戏名，在 Steam 库中搜索该游戏，右键 → 添加到对应收藏。

同一款游戏可以加入多个收藏（例如既在「低强度」又在「解压」）。

---

## 3. 动态收藏（Steam 自带）

Steam 的 **动态收藏** 是按 **Steam 商店标签 / 类型** 自动归类的，用的是 Steam 自己的数据，**不能**用我们自定义的「氛围」「强度」「一句话安利」等。

- 适合：按「类型」「标签」「是否支持手柄」等 Steam 已有信息自动分组。
- 不适合：按我们的五维分类（核心玩法/细分/氛围/强度/安利）自动分组。

---

## 4. 小结

| 方式 | 说明 |
|------|------|
| **手动建收藏 + 对照清单** | 运行 `python steam_picker.py --export-collections`，用生成的 MD 在 Steam 里建同名收藏并手动添加游戏。 |
| **动态收藏** | 仅能按 Steam 商店标签/类型自动分组，无法用我们的自定义维度。 |
| **第三方写 LevelDB** | 理论上可写 Steam 的收藏数据库，需关 Steam、格式未公开，风险高，不推荐。 |

推荐做法：用 **`--export-collections`** 拿到清单，在 Steam 里建好收藏后，按清单逐批添加；日常选游戏仍用 **`python steam_picker.py --serve`** 或命令行筛选后用 Steam 启动。
