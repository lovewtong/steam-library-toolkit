# 第二阶段：可靠性与真实场景验收

当前状态：自动化补强和可用 Windows 环境验收已完成；跨账号、跨系统及自然权益变化尚未完成，不能将整个第二阶段标记为完成。

## 任务状态与证据

| 编号 | 已完成 | 仍需验证 |
|---|---|---|
| V01 回归缺口 | 补充 ENOSPC/EACCES 刷盘失败、提交指针前真实子进程被终止、锁释放后恢复、历史时长摘要测试；原有账号/会话变化、候选隔离、损坏文件和重试测试继续通过 | 真实断电、各种文件系统与更复杂网络故障不由这些测试覆盖 |
| V02 协议样本 | 从真实 Windows 本机会话保存 5 条 RPC 字段类型观察，覆盖会话、两次客户端列表和 Web API；加入 fixture 与隐私边界测试 | 这是允许字段的结构样本，不是原始响应或完整 protobuf/分页抓包，不保留枚举值 |
| V03 多账号/设备 | 现有模拟测试拒绝错误账号、同名会话歧义及采集中会话变化；真实接口观察到两个会话，选定本机采集成功 | A→B→A 登录切换、同名设备的真人对照尚无环境 |
| V04 家庭共享 | 已有许可重叠来源测试；本次只读采集保留 owner evidence | 单次 shared 标记不能代替 Families 加入/退出或成员前后对照 |
| V05 权益变化 | 实时集合移除旧项目、许可/API 多余成员不被加入的自动化契约已覆盖 | 真实退款、撤销、免费周末到期案例待自然发生 |
| V06 离线/恢复/认证 | 本机会话采样成功；真实历史快照模式读取成功；再次在线严格采集成功；Windows 凭据存储使用临时测试值验证写入、跨进程读取、删除，测试记录已清理 | 未切换 Steam 本身的离线状态；未使真实令牌失效或测试实际 QR 重登 |
| V07 macOS/Linux | 第一期离线 CI 已在三平台通过；安全后端与非法凭据已有模拟测试 | QR、Keychain/Secret Service/KWallet、重启、锁定和删除的真人验收仍需设备 |
| V08 大库/慢网络 | 10,000 当前成员、9,000 API 项、15,000 许可候选和 12,000 快照项的合并契约通过；已有有限退避、429、认证/连接超时测试 | 未取得更大真实账号或做真实网络限速；重试预算和读超时不是任意慢速响应下的严格总下载时限 |

本轮本地完整回归：**68 项 Python、20 项 Node 测试通过**。已有第一阶段 63/17 的结果属于历史验收，不作为本轮新增代码的跨平台证明；本轮远端 CI 以对应 PR checks 为准。

## 修复的实际问题

1. `validate_token` 原先没有拒绝非有限的过期时间。现在拒绝 NaN、正负无穷，以及非 ASCII 的账号数字。该检查是本地输入校验，真实认证仍由 Steam 服务器完成。
2. 离线快照里有 361 条历史时长时，原摘要只显示“已知 0、未知 27”，漏掉历史值。现在增加 `playtime_historical` 与“历史时长”显示，不改变原来的时长状态或数值。

## 本机验证结果

- 在线结果：388 条成员，377 个 game；177 条明确零、184 条明确正时长、27 条未知。
- 离线快照模式：388 条历史候选，361 条历史时长、27 条未知；明确 `degraded` / `historical_snapshot`，输出到候选路径。
- 再次在线采集：恢复 `ok` / `client_snapshot`，成员集合与快照一致。这验证的是应用的快照模式与重新认证，不是 Steam 离线模式切换。
- Windows 系统凭据后端跨进程读写删除通过。使用独立随机测试服务名和测试值，未读取或修改项目已保存的 Steam 授权。

## 字段结构采样

```powershell
.\.venv\Scripts\python.exe tools/capture_protocol_shapes.py -o outputs/protocol-shapes.json
```

该命令显式使用 Windows 本机 Steam 会话。原始响应留在 helper 内存；输出只包括允许字段名、类型、数组长度及省略字段数量，不包括账号、机器名、游戏名、游戏 ID、时间值或令牌。默认采集不启用此功能。

样本位于 `tests/fixtures/protocol-shapes.windows.json`。允许字段之外的结构不会被导出，数组形状最多保留 32 类，因此样本不能冒充完整协议规范，也不能单独证明清单完整性。

## 后续真人验收记录要求

每项记录测试日期、平台、脱敏运行标识、操作前后状态、预期与实际结果，以及是否通过。保留原始运行在本机，不将个人库或凭据上传 CI。

- V03：提供第二个可用账号/设备后，在独立输出目录验证身份与会话选择。
- V04/V05：使用已有家庭环境或自然发生的权益变化，保存前后证据；没有事件时继续标记未验证。
- V06：在合适的实际使用时机验证 Steam 离线/重上线、自然凭据失效及 QR 重登。
- V07：在 macOS 与 Linux 桌面环境分别验收系统凭据后端。
- V08：增加可重复的网络限速/断续试验，再据结果决定是否实现更严格的响应总时限。
